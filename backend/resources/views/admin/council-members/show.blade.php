<x-app-layout><x-slot name="header"><div class="flex justify-between"><div><h1 class="text-lg font-bold">{{ $member->user->name }}</h1><p class="text-xs text-slate-500">{{ $member->council->name }}</p></div>@can('council-members.update')<a href="{{ route('admin.council-members.edit',$member) }}" class="rounded-xl bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">{{ __('Edit') }}</a>@endcan</div></x-slot><div class="mx-auto max-w-4xl px-4 py-8"><section class="rounded-2xl border bg-white p-6 shadow-sm"><div class="flex justify-between"><div><p class="text-xs font-semibold uppercase text-emerald-700">{{ __('Religious or council function') }}</p><h2 class="mt-1 text-xl font-bold">{{ __($member->function) }}</h2></div><x-status-badge :status="$member->status" /></div><p class="mt-4 rounded-xl bg-amber-50 p-3 text-sm text-amber-800">{{ __('This function is distinct from the application role.') }}</p><dl class="mt-6 grid gap-4 sm:grid-cols-2"><div><dt class="text-xs text-slate-500">{{ __('Mosque') }}</dt><dd>{{ $member->council->mosque->name }}</dd></div><div><dt class="text-xs text-slate-500">{{ __('Mandate') }}</dt><dd>{{ $member->started_at->translatedFormat('d M Y') }} — {{ $member->ended_at?->translatedFormat('d M Y') ?: __('Ongoing') }}</dd></div></dl>@if($member->responsibilities)<div class="mt-6"><h3 class="font-semibold">{{ __('Responsibilities') }}</h3><p class="mt-2 whitespace-pre-line text-sm text-slate-700">{{ $member->responsibilities }}</p></div>@endif</section></div></x-app-layout>
