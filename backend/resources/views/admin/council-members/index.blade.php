<x-app-layout><x-slot name="header"><div class="flex justify-between"><h1 class="text-lg font-bold">{{ __('Council members') }}</h1>@can('council-members.create')<a href="{{ route('admin.council-members.create') }}" class="rounded-xl bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">{{ __('Add member') }}</a>@endcan</div></x-slot><div class="mx-auto max-w-7xl space-y-5 px-4 py-8"><div class="flex gap-2"><a href="{{ route('admin.councils.index') }}" class="rounded-xl border px-4 py-2">{{ __('Councils') }}</a><a href="{{ route('admin.council-meetings.index') }}" class="rounded-xl border px-4 py-2">{{ __('Meetings') }}</a></div><section class="overflow-hidden rounded-2xl border bg-white"><div class="divide-y">@forelse($members as $member)<a href="{{ route('admin.council-members.show',$member) }}" class="flex flex-col gap-2 p-5 hover:bg-slate-50 sm:flex-row sm:items-center sm:justify-between"><span><strong>{{ $member->user->name }}</strong><small class="block text-slate-500">{{ $member->council->name }} · {{ __($member->function) }}</small></span><x-status-badge :status="$member->status" /></a>@empty<p class="p-12 text-center text-slate-500">{{ __('No members.') }}</p>@endforelse</div>@if($members->hasPages())<div class="border-t p-4">{{ $members->links() }}</div>@endif</section></div></x-app-layout>
