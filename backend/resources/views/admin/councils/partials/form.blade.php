@php($editing=isset($council))<form method="POST" action="{{ $editing?route('admin.councils.update',$council):route('admin.councils.store') }}" class="space-y-6">@csrf @if($editing)@method('PATCH')@endif @if($errors->any())<div class="rounded-xl bg-red-50 p-4 text-red-700">{{ __('Please correct the highlighted fields.') }}</div>@endif<section class="grid gap-4 rounded-2xl border bg-white p-6 shadow-sm sm:grid-cols-2">@unless($editing)<label><span class="text-sm font-semibold">{{ __('Mosque') }}</span><select name="mosque_id" required class="mt-1 w-full rounded-xl border-slate-300">@foreach($mosques as $mosque)<option value="{{ $mosque->id }}">{{ $mosque->name }}</option>@endforeach</select></label>@endunless<label class="sm:col-span-2"><span class="text-sm font-semibold">{{ __('Council name') }}</span><input name="name" required value="{{ old('name',$council->name??'') }}" class="mt-1 w-full rounded-xl border-slate-300"></label><label><span class="text-sm font-semibold">{{ __('Mandate start') }}</span><input type="date" name="mandate_start" required value="{{ old('mandate_start',isset($council)?$council->mandate_start->toDateString():'') }}" class="mt-1 w-full rounded-xl border-slate-300"></label><label><span class="text-sm font-semibold">{{ __('Mandate end') }}</span><input type="date" name="mandate_end" required value="{{ old('mandate_end',isset($council)?$council->mandate_end->toDateString():'') }}" class="mt-1 w-full rounded-xl border-slate-300"></label><label><span class="text-sm font-semibold">{{ __('Status') }}</span><select name="status" class="mt-1 w-full rounded-xl border-slate-300">@foreach(['active','inactive','dissolved','expired'] as $status)<option value="{{ $status }}" @selected(old('status',$council->status??'active')===$status)>{{ __($status) }}</option>@endforeach</select></label><label class="sm:col-span-2"><span class="text-sm font-semibold">{{ __('Notes') }}</span><textarea name="notes" rows="4" class="mt-1 w-full rounded-xl border-slate-300">{{ old('notes',$council->notes??'') }}</textarea></label></section><div class="flex justify-end gap-3"><a class="rounded-xl border px-5 py-3" href="{{ $editing?route('admin.councils.show',$council):route('admin.councils.index') }}">{{ __('Cancel') }}</a><button class="rounded-xl bg-emerald-600 px-5 py-3 font-semibold text-white">{{ __('Save') }}</button></div></form>
