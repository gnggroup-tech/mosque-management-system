<x-app-layout><x-slot name="header"><div class="flex justify-between gap-3"><div><h1 class="text-lg font-bold">{{ __('Councils') }}</h1><p class="text-xs text-slate-500">{{ __('Governance mandates and religious functions.') }}</p></div>@can('councils.create')<a href="{{ route('admin.councils.create') }}" class="rounded-xl bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">{{ __('Create council') }}</a>@endcan</div></x-slot><div class="mx-auto max-w-7xl space-y-6 px-4 py-8 sm:px-6"><div class="flex flex-wrap gap-2"><a href="{{ route('admin.councils.index') }}" class="rounded-xl bg-slate-950 px-4 py-2 text-sm text-white">{{ __('Councils') }}</a><a href="{{ route('admin.council-members.index') }}" class="rounded-xl border px-4 py-2 text-sm">{{ __('Members') }}</a><a href="{{ route('admin.council-meetings.index') }}" class="rounded-xl border px-4 py-2 text-sm">{{ __('Meetings') }}</a></div><form method="GET" class="grid gap-3 rounded-2xl border bg-white p-5 sm:grid-cols-3"><input name="search" value="{{ $filters['search'] ?? '' }}" placeholder="{{ __('Search') }}" class="rounded-xl border-slate-300"><select name="mosque_id" class="rounded-xl border-slate-300"><option value="">{{ __('All authorized mosques') }}</option>@foreach($mosques as $mosque)<option value="{{ $mosque->id }}" @selected(($filters['mosque_id']??'')==$mosque->id)>{{ $mosque->name }}</option>@endforeach</select><button class="rounded-xl bg-slate-950 px-4 py-2 text-white">{{ __('Apply filters') }}</button></form><section class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($councils as $council)<article class="rounded-2xl border bg-white p-5 shadow-sm"><div class="flex justify-between gap-3"><div><a href="{{ route('admin.councils.show',$council) }}" class="font-bold text-emerald-700">{{ $council->name }}</a><p class="text-sm text-slate-500">{{ $council->mosque->name }}</p></div><x-status-badge :status="$council->status" /></div><p class="mt-4 text-sm">{{ $council->mandate_start->translatedFormat('d M Y') }} — {{ $council->mandate_end->translatedFormat('d M Y') }}</p></article>@empty<div class="col-span-full rounded-2xl border bg-white p-12 text-center text-slate-500">{{ __('No councils found.') }}</div>@endforelse</section>{{ $councils->links() }}</div></x-app-layout>
