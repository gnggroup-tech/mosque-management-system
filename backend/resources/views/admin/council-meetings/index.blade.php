<x-app-layout><x-slot name="header"><div class="flex justify-between"><h1 class="text-lg font-bold">{{ __('Council meetings') }}</h1>@can('council-meetings.manage')<a href="{{ route('admin.council-meetings.create') }}" class="rounded-xl bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">{{ __('Plan meeting') }}</a>@endcan</div></x-slot><div class="mx-auto max-w-7xl space-y-5 px-4 py-8"><div class="flex gap-2"><a href="{{ route('admin.councils.index') }}" class="rounded-xl border px-4 py-2">{{ __('Councils') }}</a><a href="{{ route('admin.council-members.index') }}" class="rounded-xl border px-4 py-2">{{ __('Members') }}</a></div><section class="grid gap-4 md:grid-cols-2">@forelse($meetings as $meeting)<article class="rounded-2xl border bg-white p-5 shadow-sm"><div class="flex justify-between gap-3"><div><a href="{{ route('admin.council-meetings.show',$meeting) }}" class="font-bold text-emerald-700">{{ $meeting->title }}</a><p class="text-sm text-slate-500">{{ $meeting->council->name }} · {{ $meeting->council->mosque->name }}</p></div><x-status-badge :status="$meeting->status" /></div><p class="mt-4 text-sm">{{ $meeting->scheduled_at->translatedFormat('d M Y H:i') }} · {{ $meeting->participants_count }} {{ __('participants') }}</p></article>@empty<div class="col-span-full rounded-2xl border bg-white p-12 text-center text-slate-500">{{ __('No meetings.') }}</div>@endforelse</section>{{ $meetings->links() }}</div></x-app-layout>
