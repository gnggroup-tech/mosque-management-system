<x-app-layout><x-slot name="header"><h1 class="text-lg font-bold">{{ __('Plan meeting') }}</h1></x-slot><div class="mx-auto max-w-4xl px-4 py-8"><form method="POST" action="{{ route('admin.council-meetings.store') }}" class="space-y-6" x-data="{council:'{{ old('mosque_council_id',request('council_id')) }}'}">@csrf<section class="grid gap-4 rounded-2xl border bg-white p-6 shadow-sm sm:grid-cols-2"><label><span class="text-sm font-semibold">{{ __('Council') }}</span><select x-model="council" name="mosque_council_id" required class="mt-1 w-full rounded-xl border-slate-300"><option value="">{{ __('Choose council') }}</option>@foreach($councils as $council)<option value="{{ $council->id }}">{{ $council->name }}</option>@endforeach</select></label><label><span class="text-sm font-semibold">{{ __('Title') }}</span><input name="title" required value="{{ old('title') }}" class="mt-1 w-full rounded-xl border-slate-300"></label><label><span class="text-sm font-semibold">{{ __('Scheduled at') }}</span><input type="datetime-local" name="scheduled_at" required value="{{ old('scheduled_at') }}" class="mt-1 w-full rounded-xl border-slate-300"></label><label><span class="text-sm font-semibold">{{ __('Location') }}</span><input name="location" value="{{ old('location') }}" class="mt-1 w-full rounded-xl border-slate-300"></label><label><span class="text-sm font-semibold">{{ __('Quorum required') }}</span><input type="number" min="1" max="500" name="quorum_required" required value="{{ old('quorum_required',1) }}" class="mt-1 w-full rounded-xl border-slate-300"></label><label class="sm:col-span-2"><span class="text-sm font-semibold">{{ __('Agenda') }}</span><textarea name="agenda" required rows="5" class="mt-1 w-full rounded-xl border-slate-300">{{ old('agenda') }}</textarea></label></section><section class="rounded-2xl border bg-white p-6"><h2 class="font-bold">{{ __('Selected participants') }}</h2>@foreach($councils as $council)<div x-cloak x-show="council==='{{ $council->id }}'" class="mt-4 grid gap-3 sm:grid-cols-2">@forelse($council->members as $member)<label class="flex gap-3 rounded-xl border p-3"><input type="checkbox" name="participant_ids[]" value="{{ $member->id }}" @checked(in_array($member->id,old('participant_ids',[])))><span><strong>{{ $member->user->name }}</strong><small class="block text-slate-500">{{ __($member->function) }}</small></span></label>@empty<p class="text-sm text-slate-500">{{ __('No active members.') }}</p>@endforelse</div>@endforeach</section><div class="flex justify-end gap-3"><a href="{{ route('admin.council-meetings.index') }}" class="rounded-xl border px-5 py-3">{{ __('Cancel') }}</a><button class="rounded-xl bg-emerald-600 px-5 py-3 font-semibold text-white">{{ __('Create meeting') }}</button></div></form></div></x-app-layout>
