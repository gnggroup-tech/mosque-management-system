<x-app-layout><x-slot name="header"><div class="flex items-center justify-between"><div><h1 class="text-lg font-bold">{{ $faithful->first_name }} {{ $faithful->last_name }}</h1><p class="text-xs text-slate-500">{{ $faithful->registration_number }}</p></div>@can('faithful.manage')<a href="{{ route('admin.faithful.edit',$faithful) }}" class="rounded-xl bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">{{ __('Edit') }}</a>@endcan</div></x-slot><div class="mx-auto max-w-5xl px-4 py-8 sm:px-6"><div class="grid gap-6 lg:grid-cols-3"><section class="rounded-2xl border bg-white p-6 shadow-sm lg:col-span-2"><div class="flex justify-between"><h2 class="font-bold">{{ __('Personal record') }}</h2><x-status-badge :status="$faithful->status" /></div><dl class="mt-6 grid gap-5 sm:grid-cols-2">@foreach([[__('Mosque'),$faithful->mosque->name],[__('Joined at'),$faithful->joined_at?->translatedFormat('d M Y')],[__('Gender'),$faithful->gender ? __($faithful->gender) : __('Not specified')],[__('Occupation'),$faithful->occupation ?: __('Not specified')],[__('Phone'),$faithful->phone ?: __('Not specified')],[__('Email'),$faithful->email ?: __('Not specified')],[__('Address'),$faithful->address ?: __('Not specified')]] as [$label,$value])<div><dt class="text-xs font-semibold uppercase text-slate-500">{{ $label }}</dt><dd class="mt-1 break-words text-sm">{{ $value }}</dd></div>@endforeach</dl></section><aside class="rounded-2xl border border-emerald-200 bg-emerald-50 p-6"><h2 class="font-bold text-emerald-900">{{ __('Consent') }}</h2><p class="mt-3 text-sm text-emerald-800">{{ $faithful->consent_at ? __('Recorded on :date',['date'=>$faithful->consent_at->translatedFormat('d M Y H:i')]) : __('Not recorded') }}</p><p class="mt-4 text-xs text-emerald-700">{{ __('Personal data is visible only within the backend-authorized scope.') }}</p></aside></div></div></x-app-layout>
