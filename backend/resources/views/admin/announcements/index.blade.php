<x-app-layout><x-slot name="header"><div class="flex justify-between"><div><h1 class="text-lg font-bold">{{ __('Announcements') }}</h1><p class="text-xs text-slate-500">{{ __('Official internal communications available to your account.') }}</p></div>@can('announcements.manage')<a href="{{ route('admin.announcements.create') }}" class="rounded-xl bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">{{ __('Create announcement') }}</a>@endcan</div></x-slot><div class="mx-auto max-w-7xl space-y-6 px-4 py-8"><form method="GET" class="grid gap-3 rounded-2xl border bg-white p-5 sm:grid-cols-3"><input name="search" value="{{ $filters['search']??'' }}" placeholder="{{ __('Search') }}" class="rounded-xl border-slate-300"><select name="type" class="rounded-xl border-slate-300"><option value="">{{ __('All types') }}</option>@foreach(['official','prayer','meeting','event','general'] as $type)<option value="{{ $type }}" @selected(($filters['type']??'')===$type)>{{ __($type) }}</option>@endforeach</select><button class="rounded-xl bg-slate-950 px-4 py-2 text-white">{{ __('Apply filters') }}</button></form><section class="grid gap-4 md:grid-cols-2">@forelse($announcements as $announcement)@php($receipt=$announcement->receipts->first())<article class="rounded-2xl border bg-white p-5 shadow-sm"><div class="flex items-start justify-between gap-3"><div class="flex flex-wrap gap-2"><x-status-badge :status="$announcement->status" /><x-status-badge :status="$announcement->priority" />@if($receipt && !$receipt->read_at)<span class="rounded-full bg-rose-100 px-2.5 py-1 text-xs font-semibold text-rose-800">{{ __('Unread') }}</span>@endif</div><span class="text-xs text-slate-500">{{ $announcement->published_at?->translatedFormat('d M Y') }}</span></div><a href="{{ route('admin.announcements.show',$announcement) }}" class="mt-4 block text-lg font-bold text-emerald-700">{{ $announcement->title }}</a><p class="mt-2 line-clamp-3 text-sm text-slate-600">{{ $announcement->body }}</p><p class="mt-4 text-xs text-slate-500">{{ $announcement->mosque?->name ?? __('National') }} · {{ __($announcement->audience) }}@if($receipt) · {{ $receipt->available_at ? __('Available internally') : __('Pending availability') }}@endif</p></article>@empty<div class="col-span-full rounded-2xl border bg-white p-12 text-center text-slate-500">{{ __('No announcements found.') }}</div>@endforelse</section>{{ $announcements->links() }}</div></x-app-layout>
