<x-app-layout><x-slot name="header"><div class="flex justify-between"><div><h1 class="text-lg font-bold">{{ __('Activities') }}</h1><p class="text-xs text-slate-500">{{ __('Calendar and registrations in your authorized scope.') }}</p></div>@can('activities.manage')<a href="{{ route('admin.activities.create') }}" class="rounded-xl bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">{{ __('Create activity') }}</a>@endcan</div></x-slot><div class="mx-auto max-w-7xl space-y-6 px-4 py-8"><form method="GET" class="grid gap-3 rounded-2xl border bg-white p-5 sm:grid-cols-2 lg:grid-cols-4"><input name="search" value="{{ $filters['search']??'' }}" placeholder="{{ __('Search') }}" class="rounded-xl border-slate-300"><select name="mosque_id" class="rounded-xl border-slate-300"><option value="">{{ __('All authorized mosques') }}</option>@foreach($mosques as $mosque)<option value="{{ $mosque->id }}" @selected(($filters['mosque_id']??'')==$mosque->id)>{{ $mosque->name }}</option>@endforeach</select><select name="type" class="rounded-xl border-slate-300"><option value="">{{ __('All types') }}</option>@foreach(['prayer','sermon','course','religious_feast','meeting','cultural','social'] as $type)<option value="{{ $type }}" @selected(($filters['type']??'')===$type)>{{ __($type) }}</option>@endforeach</select><select name="status" class="rounded-xl border-slate-300"><option value="">{{ __('All statuses') }}</option>@foreach(['draft','published','cancelled','completed'] as $status)<option value="{{ $status }}" @selected(($filters['status']??'')===$status)>{{ __($status) }}</option>@endforeach</select><input type="date" name="from" value="{{ $filters['from']??'' }}" aria-label="{{ __('From') }}" class="rounded-xl border-slate-300"><input type="date" name="to" value="{{ $filters['to']??'' }}" aria-label="{{ __('To') }}" class="rounded-xl border-slate-300"><button class="rounded-xl bg-slate-950 px-4 py-2 text-white">{{ __('Apply filters') }}</button></form><section class="space-y-5">@forelse($activities->groupBy(fn($activity)=>$activity->starts_at->format('Y-m')) as $month=>$items)<div><h2 class="mb-3 text-sm font-bold uppercase tracking-wide text-slate-500">{{ $items->first()->starts_at->translatedFormat('F Y') }}</h2><div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@foreach($items as $activity)<article class="rounded-2xl border bg-white p-5 shadow-sm"><div class="flex justify-between gap-3"><span class="rounded-xl bg-emerald-50 px-3 py-2 text-center"><strong class="block text-lg text-emerald-800">{{ $activity->starts_at->format('d') }}</strong><small>{{ $activity->starts_at->translatedFormat('M') }}</small></span><x-status-badge :status="$activity->status" /></div><a href="{{ route('admin.activities.show',$activity) }}" class="mt-4 block font-bold text-emerald-700">{{ $activity->title }}</a><p class="mt-1 text-sm text-slate-500">{{ $activity->mosque->name }} · {{ $activity->starts_at->format('H:i') }}</p><p class="mt-3 text-xs text-slate-500">{{ $activity->registrations_count }} {{ __('registrations') }}@if($activity->capacity) / {{ $activity->capacity }}@endif</p></article>@endforeach</div></div>@empty<div class="rounded-2xl border bg-white p-12 text-center text-slate-500">{{ __('No activities found.') }}</div>@endforelse</section>{{ $activities->links() }}</div></x-app-layout>
